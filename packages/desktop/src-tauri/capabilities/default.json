{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the popover window",
  "windows": ["popover"],
  "permissions": ["core:default", "autostart:default"]
}
